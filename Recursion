
#include <bits/stdc++.h>

using namespace std;
void integer_to_binary(int n)
{
    if (n == 1)
    {
        cout << n;
    }
    else
    {
        integer_to_binary(n / 2);
        cout << n % 2;
    }
}
int main()
{

    int n;
    cin >> n;

    integer_to_binary(n);
}
